#include "EngineGUI/SkyboxPanel.h"

using namespace NoxEngine;

void NoxEngineGUI::updateSkyboxPanel(NoxEngine::GameState* game_state) {

	/*
	std::string name = PANEL_NAME_MAP[PanelName::SkyboxSettings];
	bool isplaying = false;

	// Window Begin
	ImGui::Begin(name.c_str());

	std::string texturenumber;
	texturenumber = "assets/skybox/textures/picture/bak0";
	std::vector<std::string> images
	{
		texturenumber + "/right.jpg",
		texturenumber + "/left.jpg",
		texturenumber + "/top.jpg",
		texturenumber + "/down.jpg",
		texturenumber + "/front.jpg",
		texturenumber + "/back.jpg"
	};

	game_state->renderer->setSkyBoxImages(images);

	game_state->renderer->setFrameBufferToTexture();

	


	if (ImGui::Button("Load Skybox File")) {
		
			game_state->renderer->drawSkyBox();
	
		
		
	}

	

	ImGui::End();
	*/

}
